export type SimilarProfile = {
    fullName: string;
    username: string;
    headline: string;
    profileURL: string;
    /** Medium‑sized picture (≈200×200) or '' */
    profilePicture: string;
};
